<!DOCTYPE html>
<html>
  <head>
    <title>JournalApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-indigo-900">
    <div class="flex flex-col items-center h-screen  text-white">
      <%= render 'shared/navbar' %>
      <div class="container mx-auto px-5">
        <% if notice %>
          <div class="p-4 py-2 text-sm text-green-700 bg-green-100 rounded-lg" role="notice">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="p-4 py-2 text-sm text-red-700 bg-red-100 rounded-lg" role="alert">
            <%= alert %>
          </div>
        <% end %>
      </div>
      <main class="container p-5 flex-auto overflow-y-hidden"> 
        <%= yield %>
      </main>
    </div>
  </body>
</html>
