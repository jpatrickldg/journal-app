<nav class="container mx-auto p-5 flex justify-between items-center">
  <div>
    <%= link_to 'Simple Journal', root_path, class: 'text-2xl font-bold text-gray-900 hover:text-gray-700' %>
  </div>
  <ul class="flex items center gap-10">
    
    <% if user_signed_in? %>
      <li><%= link_to 'New Journal', new_category_path, class: 'block text-blue-400 font-medium hover:text-blue-600 hover:underline' %></li>
      <li><%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'block text-blue-400 font-medium hover:text-blue-600 hover:underline' %></li>
    <% else %>
      <li>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'block text-blue-400 font-medium hover:text-blue-600 hover:underline' %>
      </li>
      <li>
        <%= link_to 'Login', new_user_session_path, class: 'block text-blue-400 font-medium hover:text-blue-600 hover:underline' %>
      </li>
    <% end %>
  </ul>
</nav>